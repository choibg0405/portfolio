module.exports = {
    // outputDir : 실제 build file들이 backend에 생성되는 경로
    outputDir: "../back-end/src/main/resources/static",

    // assetsDir : outputDir에서의 상대경로 relativePath
    // ../backend/src/main/resources/static/assets
    assetsDir: "./assets",
};
